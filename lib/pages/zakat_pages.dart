import 'package:flutter/material.dart';

class ZakatPages extends StatelessWidget {
  const ZakatPages({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}